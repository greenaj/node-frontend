sudo: required

services:
  - docker

languarge: bash 

env:
  - NodeMajor=14
  - NodeMajor=16
  - NodeMajor=17

script:
  - echo "Not implemented"

deploy:
  provider: script
  script: bash scripts/build-push.sh
  on:
    branch: master
